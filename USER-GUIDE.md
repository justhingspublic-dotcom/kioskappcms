# JustDisplay 使用說明（交接版）

> 這份文件寫給**第一次接手這套系統的人**，以及用 AI 助手（Codex／Claude）查詢的人。
> 內容＝後台網頁「使用說明」的完整版，另外補上交接需要知道的環境、路徑與排除方式。
> 最後更新：2026-09-23。畫面上的功能以後台頂欄的「使用說明」為準，兩邊內容同步維護。

---

## 1. 這套系統是什麼

JustDisplay 是一套**數位看板（電子廣告機）的雲端管理系統**。一句話說明運作方式：

> 在後台網頁上編排畫面 → 按「儲存並發布」→ 現場的展示機幾秒內自動換成新畫面。

系統由三個部分組成：

| 組成 | 是什麼 | 在哪裡 |
| --- | --- | --- |
| **管理後台** | 網頁，負責編排版面、管理機器與帳號 | `https://justdisplay.justhings.com.tw/{站台}/admin/` |
| **展示機 App** | Android App，裝在現場的直立式觸控螢幕上 | 現場機器（FarBar 43 吋為主） |
| **網頁螢幕** | 用瀏覽器全螢幕開的展示頁，效果等同一台展示機 | `https://justdisplay.justhings.com.tw/{站台}/play/` |

目前有兩個站台（各自獨立的後台、資料庫與內容）：

| 站台 | 客戶 | 後台網址 |
| --- | --- | --- |
| `joye` | 卓也小屋 | `https://justdisplay.justhings.com.tw/joye/admin/` |
| `sunrise` | 揚昇高爾夫球場 | `https://justdisplay.justhings.com.tw/sunrise/admin/` |

帳號密碼不寫在這份文件裡，請看交接包裡的「帳號清單」或伺服器上的 `.env`。

---

## 2. 名詞對照（看到這些字代表什麼）

| 名詞 | 意思 |
| --- | --- |
| **機器 / 展示機** | 一面螢幕。可能是 Android 展示機，也可能是用瀏覽器開的網頁螢幕 |
| **機器編號（DeviceId）** | 機器的唯一識別碼，例如 `farbaryh02-dfbc`；網頁螢幕會是 `web-名字` |
| **展示頁面（頁）** | 一台機器可以有多頁畫面（最多 8 頁），現場同時只展示其中一頁 |
| **大塊 / 格子** | 一頁最多 3 個大塊直向堆疊，每個大塊可再左右或上下分割一次，所以一頁最多 6 格 |
| **儲存並發布** | 把改好的內容送到機器。沒有按這顆，現場不會變 |
| **共用設定** | 做一次、套用到多台機器的版面與設定範本 |
| **雲端同步** | 機器上的開關，開了才會跟後台連動 |
| **展示模式** | 機器全螢幕播放內容的狀態（相對於「管理頁」） |
| **管理 PIN** | 從展示模式回到機器管理頁要輸入的數字密碼，預設 `0000` |
| **JustAI** | 公司的 AI 客服平台（`chat.justhings.ai`），展示機的智能客服由它提供 |
| **園區測站** | 客戶自己的環境感測器 API，提供溫度、濕度、PM2.5 等即時數值 |

---

## 3. 快速開始：把一台新機器接上來

### 3-1 Android 展示機

1. 後台側欄下方的 **機器連線資訊**，有三項可以複製：伺服器位址、連線金鑰、展示頁網址。
2. 到機器上打開 App 管理頁 → **設定 → 雲端同步**：貼上伺服器位址與連線金鑰，開啟同步，順便填一個好記的機器名稱（例如「大廳展示機」）。
   - 同一個內網可以直接按 **一鍵填入正式伺服器**，不用手打。
3. 幾秒後機器會自己出現在後台的 **機器總覽**。
4. 點該列的 **內容管理** 進工作區編排版面，按 **儲存並發布**。

### 3-2 網頁螢幕（沒有 Android 機器也能用）

1. 在要當螢幕的電腦上，開後台側欄「機器連線資訊」的 **展示頁網址**。
2. 第一次開會問「這面螢幕叫什麼名字？」，填好就開始展示。
3. 回後台 **機器總覽**，這面螢幕會跟其他機器並列，一樣點 **內容管理** 編排。
4. 瀏覽器按 F11 全螢幕；要開機自動播放，把網址設成瀏覽器的啟動頁。

補充：

- 網址加 `?device=機器名稱` 可以直接指定是哪一台，不用手填。
- 展示中**左上角連點 5 下**可以改這面螢幕的名字。
- 網頁螢幕沒有設定頁、沒有管理 PIN，也不支援休眠時段與螢幕吋數回報（機器總覽的「尺寸」欄會顯示「不支援」）。

---

## 4. 後台各頁怎麼用

### 4-1 機器總覽（登入後第一頁）

一列一台機器，縮圖是它目前展示中的畫面。

| 欄位／功能 | 說明 |
| --- | --- |
| 尺寸、解析度 | 機器自己回報的螢幕吋數與像素。網頁螢幕的尺寸顯示「不支援」，舊版 App 顯示「—」 |
| 狀態 | 在線（綠點）／離線（附多久沒回報）／尚未回報 |
| 連線檢查 | 狀態下方若出現「連到別的伺服器」＝機器填的位址不是這一站；「金鑰不符」＝金鑰打錯。兩者都到機器的雲端同步按「一鍵填入正式伺服器」 |
| 更名 | 直接改機器名稱，會同步回機器 |
| 內容管理 | 進入該機器的工作區（版面＋機器設定） |
| 批量調整展示畫面 | 右上角按鈕：勾機器 → 選一個共用版面 → 這幾台一起切到那個版面並進入展示模式。還沒有那個版面的機器，會先把版面接在現有頁面後面 |
| 重新整理 | 右上角圖示鈕；機器發布新版時清單也會自動更新 |

### 4-2 工作區 › 版面

畫布是固定的直式外框，裡面依這台機器的真實螢幕比例置中顯示。

- **展示頁面**：上方頁籤是這台機器的頁面（最多 8 頁），可新增、更名、刪除。
- **本機複製頁面**：把目前這頁複製一份接在最後面（適合做版本微調）。
- **在機器上展示此頁**：複製鈕右邊的圖示鈕，按下去機器立刻切到這一頁並進入展示模式，不必走到機器前面。
- **大塊／分割**：底部「新增大塊」增加大塊；每個大塊可分割一次（上下或左右），子格不能再分割。
- **調整比例**：拖曳分隔線，角落會即時顯示該格的實際像素。
- **交換位置**：按住格子拖到另一格放開，兩格設定互換；整個大塊換順序用右側面板的「區塊上移／下移」。
- **儲存並發布**：只影響這一台。**重新載入**會放棄未儲存的修改，拉回伺服器目前版本。

> 圖片請照角落標示的像素準備；上傳時尺寸與格子不符會提醒（可以選擇仍要上傳）。

### 4-3 工作區 › 編輯格子（點畫布上任一格）

| 設定 | 選項 |
| --- | --- |
| 背景 | 純色，或輪播圖片（每格最多 12 張、每張 3–30 秒）。天氣格另有「天氣背景」＝依即時天氣顯示動畫天空 |
| 圖片顯示方式 | **填滿裁切**（撐滿格子）或 **完整顯示**（留白處鋪同一張圖的模糊底，可再開「邊緣融合」與調整「模糊」） |
| 內容 | 無、跑馬燈（速度 50%–300%）、天氣、文字、網頁、影片（靜音循環）、園區資訊 |
| 文字顏色 | 「自動」依背景亮暗選黑或白，也可指定顏色；需要時可開「發光」讓文字在複雜背景上更清楚 |
| 點擊動作 | 無、開啟網頁、AI 智能客服、園區資訊 |
| 提示樣式 | 讓訪客知道這格可以點：無／角落徽章（右上角小小「輕觸」記號）／閃爍（內側邊框慢閃，顏色可選）／按鈕（可改文字、動態效果與排版）。預設「無」，一頁建議只用一種 |

選「AI 智能客服」時還會多出這些選項：

| 客服選項 | 說明 |
| --- | --- |
| 客服 / Agent ID | 從清單挑，或直接貼 ID（JustAI 後台網址 `agents/〔這一段〕/edit` 就是 ID） |
| 介面 | KIOSK展示模式（字大、按鈕大）或手機操作模式 |
| 主題色 | 自動（跟站台主題色）／JustAI（用客服後台設定的顏色）／指定顏色 |
| 進入時預設朗讀 | 進客服時要不要自動唸回覆；訪客可用對話頁上方喇叭鈕切換。機器沒有中文語音時不會出現那顆鈕 |
| 語速 | 唸回覆的速度 |
| 允許上傳圖片 | 關掉後客服輸入列不會出現上傳鈕 |

### 4-4 工作區 › 機器設定

| 卡片 | 說明 |
| --- | --- |
| 智能客服 API | 填 JustAI 帳號的 Email 與密碼，按「測試連線並載入客服清單」確認可用 |
| 管理 PIN | 從展示畫面回管理頁要輸入的碼。純數字、最長 8 碼，留空＝`0000`。**限管理員** |
| 閒置返回 | 訪客在客服／園區資訊／網頁頁多久沒碰就自動回展示畫面。30 秒～1 小時，預設 90 秒，可選「永不」。App 與網頁螢幕都照這個值。**限管理員** |
| 休眠時段 | 啟用排程後逐日設定「停止播放、黑畫面」的時段；開始晚於結束＝跨午夜。網頁螢幕不支援 |
| Danger Zone | 刪除這台機器（**限管理員**）。刪掉後機器畫面會停止更新 |

### 4-5 共用設定

「設計一次、一次塞進多台」。塞完之後**各機獨立**：機器裡改不動這裡，這裡改了也不動機器，要更新就再塞一次。

- **版面設定**：做好具名版面後按「加入機器」勾選機器。加入＝複製一份**接在該機現有頁面後面**並立即發布，不會覆蓋原本的頁面。機器總覽的「批量調整展示畫面」用的就是這些版面。
- **機器設定**：填好客服帳號、休眠時段、管理 PIN 與閒置返回後按「套用到機器」。套用＝**覆蓋**所選機器的這四項並立即發布，版面不受影響。

### 4-6 帳號管理（限管理員）

| 角色 | 能做什麼 |
| --- | --- |
| 管理員 | 全部功能，包含帳號管理、主題色、刪除機器、管理 PIN、閒置返回、共用客服帳號 |
| 一般帳號 | 日常上架：只看得到分配給自己的機器，可編版面、更名、改客服與休眠排程；不能動帳號、刪除機器與安全相關設定 |

完整權限對照表在「帳號管理」頁標題旁的問號。

### 4-7 頂欄工具

| 圖示 | 功能 |
| --- | --- |
| AA | 後台字體大小（中／大／特大，只影響自己這台電腦） |
| 太陽／月亮 | 淺色、深色或跟隨裝置 |
| 調色盤 | **主題色**（限管理員）：後台與展示機 App 共用一個顏色，按「儲存」後機器幾秒內跟著換 |
| 書本 | 使用說明（跟這份文件同內容） |
| 螢幕 | 開啟展示頁（用瀏覽器開這站的展示畫面） |
| 下載 App | 取得最新版 Android App 與這版的更新內容 |

---

## 5. AI 智能客服（JustAI）

展示機上的客服由 JustAI 平台提供，機器用一組 JustAI 帳號登入，就能使用該帳號底下訓練好的客服。

1. 到 <https://chat.justhings.ai/login> 用 Email 註冊（建議用公司共用信箱）。
2. 在 JustAI 後台**建立客服代理人**、上傳知識庫訓練，需要訪客傳圖就開啟上傳權限，最後**發布**。
3. 回後台 → 機器的 **機器設定 › 智能客服 API** → 填 Email 與密碼 → **測試連線並載入客服清單**。
4. 到版面的格子，**點擊動作** 選 **AI 智能客服**，挑客服，儲存並發布。

多台機器共用同一組帳號時，用「共用設定 › 機器設定」填一次、一次套用到多台。

---

## 6. 園區資訊（有環境感測器的場域才有）

園區裝了環境感測器時，可以讓訪客在機器上看園區地圖與各測站的即時數值。站台沒設定園區測站 API 時，這些選項不會出現。

| 設定 | 說明 |
| --- | --- |
| 放一格入口 | 格子的「內容」選 **園區資訊**（該格只顯示標題），「點擊動作」也選 **園區資訊** |
| 測站 API | 填客戶感測器的資料網址；留白時用站台預設值 |
| 目前位置 | 選這台機器擺在哪一站，地圖會標「目前位置」，訪客點進來預設先看這一站 |
| 園區資訊版面 | KIOSK展示模式（頂欄與文字放大）或手機操作模式 |
| 按鈕樣式 | 按鈕文字（預設「點我查看」）、動態效果、排版（自動／橫排／直排） |
| 天氣格用測站數值 | 內容選「天氣」時，來源改成「園區測站」即可顯示某站數值或輪播全部測站 |

測站數值每 30 秒更新；設成輪播時機器每 10 秒換一站。

---

## 7. 機器現場（Android App）

| 操作 | 說明 |
| --- | --- |
| 開始展示 | 按「開始展示」進入全螢幕播放；左右滑動預覽可切換要播的頁面 |
| 回到管理頁 | 進入展示後 6 秒內點右上角齒輪、**左上角連點 5 下**、或按系統返回，都要輸入管理 PIN |
| 訪客動線 | 訪客點格子開啟的網頁、客服與園區資訊頁，閒置到設定時間就回展示畫面，客服對話同時重置 |
| 客服朗讀 | 對話頁上方喇叭鈕可隨時開關；預設開或關由後台那一格決定 |
| 立即同步 | 設定 › 雲端同步 › 立即同步，可手動拉一次設定 |
| 一鍵填入正式伺服器 | 自動帶入正確的伺服器位址與連線金鑰 |
| 軟體更新 | 設定 › 軟體更新，檢查並安裝新版；原有場域與展示設定會保留 |
| 休眠診斷 | 休眠時段沒關螢幕時，到「休眠時段」執行「硬體診斷」與「測試關閉螢幕」確認機器支援 |

---

## 8. 常見問題排除

| 症狀 | 先檢查什麼 |
| --- | --- |
| 機器顯示離線 | 機器網路；雲端同步的位址與金鑰是否與後台側欄相同。機器約每半分鐘回報一次 |
| 狀態下方有提示字 | 「連到別的伺服器」或「金鑰不符」＝機器連線設定不對，到機器雲端同步按「一鍵填入正式伺服器」 |
| 發布後畫面沒變 | 確認機器在線，且機器上按過「開始展示」；仍沒變就在機器按「立即同步」 |
| 客服無法連線 | 機器設定按「測試連線」確認帳密。憑證錯誤時測試結果會附機器時間與憑證資訊（多半是機器日期跑掉） |
| 客服沒有附件按鈕 | 該格的「允許上傳圖片」關著，或該代理人在 JustAI 後台沒開上傳權限 |
| 客服不會唸回覆 | 對話頁上方喇叭鈕是否開著；整顆鈕沒出現＝那台機器沒安裝中文語音 |
| 找不到園區資訊選項 | 這個站台還沒設定園區測站 API |
| 忘記管理 PIN | 管理員在後台的機器設定重設一組並儲存發布 |
| 後台預覽看不到影片 | 正常，預覽畫布不播影片；機器與網頁螢幕會正常播放 |
| 兩個人同時改一台 | 以最後儲存的人為準，建議先按「重新載入」再改 |

---

## 9. 系統與環境（工程／維運用）

### 9-1 專案位置

| 專案 | 路徑 | 說明 |
| --- | --- | --- |
| 管理後台 | `c:\Code\KioskAdmin` | Node.js + Express + SQL Server；網頁在 `public/`，API 在 `src/server.js` |
| 展示機 App | `c:\Code\KioskApp` | Android（Kotlin / Jetpack Compose） |

### 9-2 正式環境

| 項目 | 內容 |
| --- | --- |
| 伺服器 | `192.168.1.82`（JTWEB2），IIS 反向代理到 Node |
| 站台根目錄 | `D:\WebSite\JustDisplay\KioskAdmin` |
| 資料庫 | SQL Server `192.168.1.92`；正式 `KioskAdmin`、開發 `KioskAdminDev` |
| 開機自啟 | 工作排程器：`KioskAdmin (JustDisplay)`、`KioskAdmin (Sunrise)` |
| 系統 log | 站台目錄下 `logs/app-日期.log`；健康狀態看 `/api/health` |
| 部署方式 | 見 `DEPLOY.md`（SMB 蓋檔 → 重啟排程工作 → 驗證） |
| API 文件 | 後台網址 `/docs`（Swagger UI），定義在 `docs/openapi.yaml`，另見 `API.md` |

### 9-3 資料放哪

- 機器設定與版面：SQL Server `KioskConfig` 資料表（整份 JSON）。
- 圖片與影片：站台目錄 `uploads/`（揚昇為 `uploads-sunrise/`），網頁上以 `/files/檔名` 取用。
- 操作紀錄與機器事件：`KioskAuditLog`、`KioskDeviceEvent` 兩張表（客戶後台不顯示，需要時用 SQL 或 API 查）。

### 9-4 後台「使用說明」怎麼維護

- 內容在 `public/index.html` 的 `#guideModal` 區塊（左側章節導覽＋右側內容）。
- 截圖在 `public/img/manual/`，用 `tools/uitest/shoot-manual.js` 產生：它會攔截 API、餵 `tools/uitest/manual-demo.js` 的示範資料，**不連資料庫、不會動到任何站台的內容**。
- 改完內容記得同步更新這份 `USER-GUIDE.md`。

---

## 10. 相關文件

| 文件 | 內容 |
| --- | --- |
| `DEPLOY.md` | 部署與伺服器操作 |
| `API.md` / `docs/openapi.yaml` | API 規格 |
| 後台頂欄「使用說明」 | 這份文件的網頁版（含截圖） |
